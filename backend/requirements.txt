# ─────────────────────────────────────────────────────────────
#  ASX Screener — Backend Dependencies
# ─────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
asyncpg==0.30.0          # Async PostgreSQL driver
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0          # DB migrations (schema changes post-launch)
psycopg2-binary==2.9.10  # Sync driver (for scripts/Airflow)

# Cache
redis[hiredis]==5.2.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# Validation
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# HTTP client (for fetching ASX data)
httpx==0.28.1
aiohttp==3.11.10
requests==2.32.3         # Used by data loader scripts

# Data providers
eodhd                    # Official EODHD Python SDK (pip install eodhd -U)

# Data processing
pandas==2.2.3
numpy==2.2.0
pyarrow==18.1.0

# AI / Embeddings
anthropic==0.40.0
openai==1.58.1           # For embeddings (text-embedding-3-small)

# Payments
stripe==11.3.0

# Email
resend==2.5.0

# AWS S3
boto3==1.35.84

# Scheduling
apscheduler==3.10.4

# Monitoring
prometheus-fastapi-instrumentator==7.0.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1            # For TestClient
